import 'package:fluent_ui/fluent_ui.dart';

class GuardrailsPage extends StatelessWidget {
  const GuardrailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Guardrails Page'));
  }
}