import 'package:fluent_ui/fluent_ui.dart';

class SanctuaryPage extends StatelessWidget {
  const SanctuaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Sanctuary Page'));
  }
}